# LiveKit WebSocket URL (optional)
# If not set, the frontend will fetch this from the server's /api/v1/config endpoint
# This allows the same frontend build to work with different server configurations
# Override here only for local development or testing
# VITE_LIVEKIT_URL=ws://localhost:7880
